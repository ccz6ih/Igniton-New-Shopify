{{ 'component-ig-products-showcase.css' | asset_url | stylesheet_tag }}

{%- style -%}
  #IgProductsShowcase-{{ section.id }} .ig-products-showcase__title {
    font-size: calc(var(--font-heading-scale) * {{ section.settings.heading_size }}rem);
  }
  
  #IgProductsShowcase-{{ section.id }} .ig-products-showcase__card {
    font-size: calc(1rem * {{ section.settings.text_size | divided_by: 100.0 }});
  }
{%- endstyle -%}

<section 
  id="IgProductsShowcase-{{ section.id }}" 
  class="ig-products-showcase color-{{ section.settings.color_scheme }}"
>
  <div class="page-width">
    {%- if section.settings.eyebrow != blank -%}
      <p class="ig-section-label">{{ section.settings.eyebrow }}</p>
    {%- endif -%}
    
    {%- if section.settings.heading != blank -%}
      <h2 class="ig-products-showcase__title ig-section-title ig-font-display">
        {{ section.settings.heading }}
      </h2>
    {%- endif -%}
    
    {%- liquid
      assign regular_cards = section.blocks | where: 'type', 'product_card'
      assign featured_cards = ''
      assign non_featured_cards = ''
    -%}
    
    <div class="ig-products-showcase__layout">
      <div class="ig-products-showcase__grid">
        {%- for block in section.blocks -%}
          {%- if block.type == 'product_card' and block.settings.featured != true -%}
            {%- liquid
              assign product = block.settings.product
              assign product_url = block.settings.custom_link
              if product != blank
                assign product_url = product.url
              endif
            -%}
            
            <a 
              href="{{ product_url }}" 
              class="ig-products-showcase__card{% if forloop.first %} is-active{% endif %}"
              {{ block.shopify_attributes }}
              data-product-id="{{ block.id }}"
              data-product-image="{% if product.featured_image %}{{ product.featured_image | image_url: width: 1200 }}{% endif %}"
            >
                {%- if block.settings.tag != blank -%}
                  <p class="ig-products-showcase__tag">{{ block.settings.tag }}</p>
                {%- endif -%}
                
                <h3 class="ig-products-showcase__name ig-font-display">
                  {%- if block.settings.custom_name != blank -%}
                    {{ block.settings.custom_name }}
                  {%- elsif product != blank -%}
                    {{ product.title }}
                  {%- else -%}
                    Product name
                  {%- endif -%}
                </h3>
                
                {%- if block.settings.custom_description != blank or product.description != blank -%}
                  <div class="ig-products-showcase__desc ig-font-body">
                    {%- if block.settings.custom_description != blank -%}
                      {{ block.settings.custom_description }}
                    {%- elsif product != blank -%}
                      {{ product.description | strip_html | truncatewords: 30 }}
                    {%- endif -%}
                  </div>
                {%- endif -%}
              
                {%- if block.settings.show_stats -%}
                  <div class="ig-products-showcase__stats">
                    {%- for i in (1..3) -%}
                      {%- assign stat_num_key = 'stat_' | append: i | append: '_num' -%}
                      {%- assign stat_label_key = 'stat_' | append: i | append: '_label' -%}
                      {%- assign stat_num = block.settings[stat_num_key] -%}
                      {%- assign stat_label = block.settings[stat_label_key] -%}
                      
                      {%- if stat_num != blank -%}
                        <div class="ig-products-showcase__stat">
                          <span class="ig-products-showcase__stat-num">{{ stat_num }}</span>
                          <span class="ig-products-showcase__stat-label">{{ stat_label }}</span>
                        </div>
                      {%- endif -%}
                    {%- endfor -%}
                  </div>
                {%- endif -%}
                
                {%- if block.settings.show_price and product != blank -%}
                  <p class="ig-products-showcase__price">
                    From <strong>{{ product.price | money }}</strong>
                    {%- if block.settings.price_note != blank -%}
                      · {{ block.settings.price_note }}
                    {%- endif -%}
                  </p>
                {%- endif -%}
                
                {%- if block.settings.link_text != blank -%}
                  <span class="ig-products-showcase__link">
                    {{ block.settings.link_text }} <span>→</span>
                  </span>
                {%- endif -%}
              </a>
          {%- endif -%}
        {%- endfor -%}
      </div>
      
      <div class="ig-products-showcase__image-display">
        {%- assign first_non_featured = true -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'product_card' and block.settings.featured != true -%}
            {%- liquid
              assign product = block.settings.product
              assign product_image = product.featured_image
            -%}
            
            {%- if product_image != blank -%}
              <div 
                class="ig-products-showcase__image{% if first_non_featured %} is-active{% endif %}"
                data-product-id="{{ block.id }}"
              >
                {{ product_image | image_url: width: 1200 | image_tag: loading: 'lazy', class: 'ig-products-showcase__img', alt: product.title }}
              </div>
              {%- assign first_non_featured = false -%}
            {%- endif -%}
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>
    
    {%- comment -%} Featured Bundle Card (Full Width Below) {%- endcomment -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'product_card' and block.settings.featured == true -%}
        {%- liquid
          assign product = block.settings.product
          assign product_url = block.settings.custom_link
          if product != blank
            assign product_url = product.url
          endif
        -%}
        
        <a 
          href="{{ product_url }}" 
          class="ig-products-showcase__card ig-products-showcase__card--featured"
          {{ block.shopify_attributes }}
          style="background: {{ block.settings.featured_bg }};"
        >
          {%- if block.settings.tag != blank -%}
            <p class="ig-products-showcase__tag">{{ block.settings.tag }}</p>
          {%- endif -%}
          
          <h3 class="ig-products-showcase__name ig-font-display">
            {%- if block.settings.custom_name != blank -%}
              {{ block.settings.custom_name }}
            {%- elsif product != blank -%}
              {{ product.title }}
            {%- else -%}
              Product name
            {%- endif -%}
          </h3>
          
          {%- if block.settings.custom_description != blank or product.description != blank -%}
            <div class="ig-products-showcase__desc ig-font-body">
              {%- if block.settings.custom_description != blank -%}
                {{ block.settings.custom_description }}
              {%- elsif product != blank -%}
                {{ product.description | strip_html | truncatewords: 30 }}
              {%- endif -%}
            </div>
          {%- endif -%}
          
          {%- if block.settings.show_stats -%}
            <div class="ig-products-showcase__stats ig-products-showcase__stats--featured">
              {%- for i in (1..3) -%}
                {%- assign stat_num_key = 'stat_' | append: i | append: '_num' -%}
                {%- assign stat_label_key = 'stat_' | append: i | append: '_label' -%}
                {%- assign stat_num = block.settings[stat_num_key] -%}
                {%- assign stat_label = block.settings[stat_label_key] -%}
                
                {%- if stat_num != blank -%}
                  <div class="ig-products-showcase__stat">
                    <span class="ig-products-showcase__stat-num">{{ stat_num }}</span>
                    <span class="ig-products-showcase__stat-label">{{ stat_label }}</span>
                  </div>
                {%- endif -%}
              {%- endfor -%}
            </div>
          {%- endif -%}
          
          {%- if block.settings.show_price and product != blank -%}
            <p class="ig-products-showcase__price">
              From <strong>{{ product.price | money }}</strong>
              {%- if block.settings.price_note != blank -%}
                · {{ block.settings.price_note }}
              {%- endif -%}
            </p>
          {%- endif -%}
          
          {%- if block.settings.link_text != blank -%}
            <span class="ig-products-showcase__link">
              {{ block.settings.link_text }} <span>→</span>
            </span>
          {%- endif -%}
        </a>
      {%- endif -%}
    {%- endfor -%}
  </div>
</section>

<script>
(function() {
  const showcaseSection = document.querySelector('#IgProductsShowcase-{{ section.id }}');
  if (!showcaseSection) return;
  
  const cards = showcaseSection.querySelectorAll('.ig-products-showcase__card:not(.ig-products-showcase__card--featured)');
  const images = showcaseSection.querySelectorAll('.ig-products-showcase__image');
  
  cards.forEach(card => {
    card.addEventListener('mouseenter', () => {
      const productId = card.dataset.productId;
      
      // Remove active class from all cards and images
      cards.forEach(c => c.classList.remove('is-active'));
      images.forEach(img => img.classList.remove('is-active'));
      
      // Add active class to current card and matching image
      card.classList.add('is-active');
      const matchingImage = showcaseSection.querySelector(`.ig-products-showcase__image[data-product-id="${productId}"]`);
      if (matchingImage) {
        matchingImage.classList.add('is-active');
      }
    });
  });
})();
</script>

{% schema %}
{
  "name": "Products Showcase",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "default": "Two products. One breakthrough.",
      "label": "Eyebrow text"
    },
    {
      "type": "textarea",
      "id": "heading",
      "default": "Every ingredient enhanced for maximum effect.",
      "label": "Heading"
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "heading_size",
      "min": 2,
      "max": 5,
      "step": 0.5,
      "default": 3,
      "unit": "rem",
      "label": "Heading size"
    },
    {
      "type": "range",
      "id": "text_size",
      "min": 80,
      "max": 140,
      "step": 5,
      "default": 100,
      "unit": "%",
      "label": "Text size"
    }
  ],
  "blocks": [
    {
      "type": "product_card",
      "name": "Product Card",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product",
          "info": "Product image will be shown on right when hovered"
        },
        {
          "type": "checkbox",
          "id": "featured",
          "default": false,
          "label": "Featured card (bundle)",
          "info": "Makes card full-width and places it below the grid"
        },
        {
          "type": "color",
          "id": "featured_bg",
          "label": "Featured card background",
          "default": "#fdfaf5"
        },
        {
          "type": "header",
          "content": "Content"
        },
        {
          "type": "text",
          "id": "tag",
          "default": "Cognition",
          "label": "Product tag"
        },
        {
          "type": "text",
          "id": "custom_name",
          "label": "Custom name",
          "info": "Leave blank to use product title"
        },
        {
          "type": "textarea",
          "id": "custom_description",
          "label": "Custom description",
          "info": "Leave blank to use product description"
        },
        {
          "type": "header",
          "content": "Stats"
        },
        {
          "type": "checkbox",
          "id": "show_stats",
          "default": true,
          "label": "Show stats"
        },
        {
          "type": "text",
          "id": "stat_1_num",
          "default": "100%",
          "label": "Stat 1 - Number"
        },
        {
          "type": "text",
          "id": "stat_1_label",
          "default": "Memory improvement",
          "label": "Stat 1 - Label"
        },
        {
          "type": "text",
          "id": "stat_2_num",
          "default": "83%",
          "label": "Stat 2 - Number"
        },
        {
          "type": "text",
          "id": "stat_2_label",
          "default": "Performance quality",
          "label": "Stat 2 - Label"
        },
        {
          "type": "text",
          "id": "stat_3_num",
          "default": "30 days",
          "label": "Stat 3 - Number"
        },
        {
          "type": "text",
          "id": "stat_3_label",
          "default": "To measurable results",
          "label": "Stat 3 - Label"
        },
        {
          "type": "header",
          "content": "Price and link"
        },
        {
          "type": "checkbox",
          "id": "show_price",
          "default": true,
          "label": "Show product price"
        },
        {
          "type": "text",
          "id": "price_note",
          "default": "Subscribe & save 20%",
          "label": "Price note"
        },
        {
          "type": "url",
          "id": "custom_link",
          "label": "Custom link",
          "info": "Used if no product selected"
        },
        {
          "type": "text",
          "id": "link_text",
          "default": "Shop Now",
          "label": "Link text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Products Showcase",
      "blocks": [
        {
          "type": "product_card",
          "settings": {
            "tag": "Cognition",
            "custom_name": "IgniCognition™",
            "custom_description": "Clinically proven to improve memory, focus, and mental speed — without stimulants or the inevitable crash. Your sharpest thinking, sustained throughout the day.",
            "stat_1_num": "100%",
            "stat_1_label": "Memory improvement",
            "stat_2_num": "83%",
            "stat_2_label": "Performance quality",
            "stat_3_num": "30 days",
            "stat_3_label": "To measurable results",
            "link_text": "Shop IgniCognition"
          }
        },
        {
          "type": "product_card",
          "settings": {
            "tag": "Longevity",
            "custom_name": "IgniLongevity™",
            "custom_description": "Targets the inflammatory and aging biomarkers that predict your 10-year health trajectory. Support your body's natural repair systems — so you don't just live longer, you feel better doing it.",
            "stat_1_num": "hs-CRP",
            "stat_1_label": "Inflammation marker",
            "stat_2_num": "NAD⁺",
            "stat_2_label": "Cellular energy support",
            "stat_3_num": "60 days",
            "stat_3_label": "To longevity results",
            "link_text": "Shop IgniLongevity"
          }
        },
        {
          "type": "product_card",
          "settings": {
            "tag": "Best Value",
            "custom_name": "The Complete Stack — Cognition + Longevity",
            "custom_description": "Both formulas together for comprehensive brain and body support. The most complete approach to human performance and longevity.",
            "show_stats": false,
            "link_text": "Shop the Bundle",
            "featured": true
          }
        }
      ]
    }
  ]
}
{% endschema %}
